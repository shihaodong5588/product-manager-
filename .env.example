# 数据库配置

# 生产环境：使用 Supabase PostgreSQL (推荐)
# 获取方式: https://supabase.com -> 创建项目 -> Project Settings -> Database -> Connection String
#
# ⚠️ 重要：使用 Transaction Pooler (port 6543) 而不是 Session Pooler (port 5432)
# Transaction Pooler 支持更多并发连接，避免连接池耗尽错误
# 必须添加 pgbouncer=true 和 connection_limit=1 参数
DATABASE_URL="postgresql://postgres.[YOUR-PROJECT-REF]:[YOUR-PASSWORD]@aws-1-ap-northeast-2.pooler.supabase.com:6543/postgres?pgbouncer=true&connection_limit=1"

# Direct Connection - 用于 Prisma 迁移（运行 npx prisma db push 时使用）
DIRECT_URL="postgresql://postgres:[YOUR-PASSWORD]@db.[YOUR-PROJECT-REF].supabase.co:5432/postgres"

# 本地开发：可以使用 SQLite (切换 prisma/schema.prisma 的 provider)
# DATABASE_URL="file:./prisma/dev.db"

# 或使用本地 PostgreSQL
# DATABASE_URL="postgresql://user:password@localhost:5432/product_manager"

# Gemini API 配置
# 获取方式: https://aistudio.google.com/app/apikey
GEMINI_API_KEY=your_gemini_api_key_here

# Gemini 模型选择（可选）
# 可选值: gemini-2.5-flash | gemini-2.5-pro | gemini-2.5-flash-lite
# 默认: gemini-2.5-flash
GEMINI_MODEL=gemini-2.5-flash
